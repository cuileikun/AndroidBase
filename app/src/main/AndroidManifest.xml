<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cuileikun.androidbase">

    <!-- 拨打电话权限 add by clk on -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <!-- 拨打电话权限 add by clk off -->
    <!-- 短信验证需要的权限start -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.GET_TASKS" />

    <!-- 短信验证需要的权限stop -->
    <!-- 发送短信需要的权限 -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.AndroidBaseActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--<activity android:name=".activity.AndroidBaseActivity" />-->

        <activity android:name=".activity.AndroidBaseFirstActivity" />

        <activity android:name=".activity.AndroidBaseSecondActivity" />
        <activity android:name=".activity.DoXmlActivity" />
        <activity android:name=".activity.UserLoginDataActivity" />
        <activity android:name=".activity.FilePermissionActivity" />
        <activity android:name=".activity.UserLoginSdcardActivity" />
        <activity android:name=".activity.GetSdcardSizeActivity" />
        <activity android:name=".activity.UserLoginSpActivity" />
        <activity android:name=".activity.LogActivity" />

        <activity android:name=".activity.AndroidBaseThirdActivity" />
        <activity android:name=".activity.SqliteActivity" />
        <activity android:name=".activity.ListviewActivity" />
        <activity android:name=".activity.ContactActivity" />
        <activity android:name=".activity.ArrayAdapterActivity" />

        <activity android:name=".activity.AndroidBaseFouthActivity" />
        <activity android:name=".activity.YuanMaActivity" />
        <activity android:name=".activity.AnrActivity" />
        <activity android:name=".activity.LookPictureActivity" />
        <activity android:name=".activity.ModifyLookPictureActivity" />
        <activity android:name=".activity.HandlerActivity" />
        <activity android:name=".activity.NewsActivity" />
        <activity android:name=".activity.SmartImageViewActivity" />
    </application>

</manifest>